{
  "name": "@greecon/edge-simulator",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@greecon/shared": "0.1.0",
    "mqtt": "^5.10.1"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
